spring:
  application:
    name: caseservice
  cloud:
    config:
      uri: http://localhost:8888
    consul:
      host: localhost
      port: 8500
      discovery:
        instanceId: ${spring.application.name}:${random.value}
management:
  security:
    enabled: false
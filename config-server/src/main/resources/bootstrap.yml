spring:
  application:
    name: configserver
  cloud:
    config:
      server:
        git:
          uri: file://C:/edu/microservices
    consul:
      host: localhost
      port: 8500
      discovery:
        instanceId: ${spring.application.name}:${random.value}